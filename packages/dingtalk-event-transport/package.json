{
	"name": "cyrus-dingtalk-event-transport",
	"version": "0.2.66",
	"description": "DingTalk event transport for receiving and sending DingTalk robot messages via Stream mode (WebSocket)",
	"type": "module",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"files": [
		"dist"
	],
	"scripts": {
		"build": "tsc",
		"dev": "tsc --watch",
		"typecheck": "tsc --noEmit",
		"test": "vitest",
		"test:run": "vitest run"
	},
	"dependencies": {
		"cyrus-core": "workspace:*",
		"dingtalk-stream-sdk-nodejs": "^2.0.4"
	},
	"devDependencies": {
		"@types/node": "^20.0.0",
		"typescript": "^5.3.3",
		"vitest": "^4.1.0"
	},
	"publishConfig": {
		"access": "public"
	}
}
